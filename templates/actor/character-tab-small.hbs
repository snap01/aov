<nav class="aov characterSmall actor sheet-tabs tabs" aria-roledescription="{{localize 'SHEETS.FormNavLabel'}}">
  {{#each tabs as |tab|}}
  <a class="{{tab.cssClass}}" data-action="tab" data-group="{{tab.group}}" data-tab="{{tab.id}}">
    {{#if tab.label}}<label class="">{{localize tab.label}}</label>{{/if}}
  </a>
  {{/each}}
</nav>